## tp-test performance test

```bash
cd sqlgen
go test -test.run TestPerformance
```

Results will be saved in `results` file. 

Prepare data will be saved in `preapre_sql.sql`.

query sql will be saved in `query_sql.sql`. 